{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: current file (McRemote)",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "cwd": "${fileDirname}",
      "justMyCode": true
    }
  ]
}
